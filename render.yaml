services:
  - type: web
    name: ist-voice-agent
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: SKIP_VECTOR_INDEX
        value: "0"
